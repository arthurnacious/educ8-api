{
  "name": "api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db": "bun drizzle-kit generate && bun drizzle-kit migrate",
    "generate": "bun drizzle-kit generate",
    "migrate": "bun drizzle-kit migrate",
    "seed": "bun run src/db/seeds/index.ts"
  },
  "dependencies": {
    "crypto-js": "^4.2.0",
    "drizzle-orm": "^0.40.0",
    "hono": "^4.7.1",
    "pg": "^8.13.3",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@faker-js/faker": "^9.5.0",
    "@types/bun": "latest",
    "@types/pg": "^8.11.11",
    "drizzle-kit": "^0.30.5"
  }
}
